# 后端规范 README

## 1. 文件定位

本目录承接跨版本复用的后端设计与实现规范。

## 2. 当前默认口径

1. 新表优先只存主数据代码和本表新增信息，不冗余存显示名称。
2. API 入参默认要求显式校验，关键执行路径默认补充安全校验。
3. 删除动作默认走业务校验，不直接物理删除主数据。
4. 时间存储精度由后端和数据库决定，业务页面展示精度由前端规范控制。

## 3. 后续扩展建议

1. 可在本目录继续补充 API 设计规范。
2. 可在本目录继续补充数据库命名、审计字段、安全基线等规则。
