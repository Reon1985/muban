# 前端 UI 技术实现文档 v1

## 1. 文件定位

本文件用于定义当前项目默认 UI 设计与实现基线。

## 2. 当前默认基线

1. 页面标题、搜索区、列表区、详情区、弹窗样式必须统一。
2. 搜索组件需要按数据规模区分普通下拉、远程搜索和树形选择。
3. 所有页面按钮文案必须显示业务文案，不允许直接显示 i18n key。
4. 业务页面中的日期默认只展示到年月日，后端存储精度不在本文件修改。

## 3. 后续维护方式

1. 如果新项目形成了稳定视觉基线，请直接覆盖本文件中的默认基线章节。
2. 如果进入下一轮稳定规范，可新增 `v2` 文档，不直接覆写旧版本文件名。
