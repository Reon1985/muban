# 页面点击与业务校验测试 README

## 1. 文件定位

本目录承接页面按钮、弹窗、表单提交、删除校验、权限反馈等业务交互测试方法。

## 2. 默认检查范围

1. 新增、编辑、删除、提交、导出等按钮是否可用。
2. 无权限时按钮是否展示灰态与提示。
3. 删除、失效、状态流转是否具备业务校验。
4. 表单字段校验、只读规则、默认值规则是否符合需求。

## 3. 输出要求

1. 每轮修复应覆盖本轮功能点按钮点击验证。
2. 发现通用交互问题时，应回填到 UI 规范或错误检查清单。
