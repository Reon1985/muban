interface:
  display_name: "项目文档治理与实施门禁"
  short_description: "按项目文档分层路由正式文档并执行实施门禁与评审闭环"
  default_prompt: "Use $project-doc-governance as the repository adapter to route formal outputs into the correct doc layer, enforce preview-first and implementation-gate rules, and keep formal docs as the authority source rather than creating parallel rules in the skill."

policy:
  allow_implicit_invocation: true
